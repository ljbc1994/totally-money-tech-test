/**
 * NOTE:
 * For simplicity I've got a config list, but for sensitive
 * data, it's best to inject these either through a CI or a
 * deploy build step interpolator.
 */
export const CARD_SERVICE_API_URL = "an.example.api.service";
